PUBLIC_API_URL=http://localhost:5000

# Backend-dəki SECRET_KEY (backend/.env) ilə EYNİ olmalıdır - JWT imzasını
# server-side yoxlamaq üçün (bax: hooks.server.js). Yalnız server-də
# istifadə olunur, brauzerə heç vaxt getmir.
JWT_SECRET_KEY=
